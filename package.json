{
  "name": "wordle-explore",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lc": "tsc --outDir dist && node dist/letter-count.js",
    "build": "tsc --outDir dist"
  },
  "author": "mktoho12",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^17.0.18",
    "typescript": "^4.5.5",
    "word-list": "^3.1.0"
  }
}
